import { TableCellEnumPipe } from './table-cell-enum.pipe';

describe('TableCellEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new TableCellEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
